<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Create a form view for mymodel -->
        <record id="view_form_mymodel" model="ir.ui.view">
            <field name="name">mymodel.form</field>
            <field name="model">my_module.mymodel</field>
            <field name="arch" type="xml">
                <form string="MyModel Form">
                    <header>
                        <button name="set_amount" string="Set Amount" type="object" class="oe_highlight"/>
                    </header>
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="amount"/>
                            <field name="active"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        <!-- omitted action and menu item for brevity -->
    </data>
</odoo>
