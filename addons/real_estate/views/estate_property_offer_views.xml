<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_estate_property_offer_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree string="Offers">
                <field name="partner_id"/>
                <field name="price"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Offer">
                <header>
                    <button name="action_accept" type="object" icon="fa-thumbs-up" attrs="{'invisible': [('state', '!=', 'new')]}" />
                    <button name="action_refuse" type="object" icon="fa-thumbs-down" attrs="{'invisible': [('state', '!=', 'new')]}" />
                </header>
                <sheet>
                    <group>
                        <field name="partner_id"/>
                        <field name="price"/>
                        <field name="property_id"/>
                        <field name="state"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_accept" type="object" string="Accept" class="btn-primary" attrs="{'invisible': [('state', '!=', 'new')]}" />
                    <button name="action_refuse" type="object" string="Refuse" class="btn-secondary" attrs="{'invisible': [('state', '!=', 'new')]}" />
                </footer>
            </form>
        </field>
    </record>
</odoo>
